# Server
PORT=8080
APP_BASE_URL=http://localhost:8080
INTERNAL_FILE_DIR=./files

# API access for Tools/Actions (comma-separated keys allowed)
API_KEY_ALLOWLIST=dev_local_key_123,prod_key_123

# Optional: switch to Supabase signed URLs for PDFs (recommended in prod)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE=
SUPABASE_BUCKET=jsa-pdfs

# Optional: if you later enable true server-side vision calls
OPENAI_API_KEY=

# Server
PORT=8080
APP_BASE_URL=http://localhost:8080
INTERNAL_FILE_DIR=./files

# API access (comma-separated keys allowed)
API_KEY_ALLOWLIST=dev_local_key_123

# Optional: Supabase (for signed URLs in prod)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE=
SUPABASE_BUCKET=jsa-pdfs

# Optional: future server-side vision
OPENAI_API_KEY=
